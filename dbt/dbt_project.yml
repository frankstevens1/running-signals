name: running_signals
version: "1.0.0"
config-version: 2

profile: running_signals

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  running_signals:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table