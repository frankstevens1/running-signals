-- TASK: Split all runs into 4 quartiles by distance and compute the average pace per quartile.
-- Show quartile (1-4), run count, avg distance, and avg pace. Order by quartile.
-- TABLE: site_runs
-- HINT: Use NTILE(4) OVER (ORDER BY distance_km) in a subquery or CTE, then GROUP BY quartile.

-- YOUR SOLUTION BELOW:
