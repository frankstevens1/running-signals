-- TASK: Use a CTE to calculate total distance per month, then select months with more than 50km.
-- Show month (truncated to month), total_km. Order by month.
-- TABLE: site_runs
-- HINT: WITH monthly AS (...) SELECT * FROM monthly WHERE total_km > 50.

-- YOUR SOLUTION BELOW:
