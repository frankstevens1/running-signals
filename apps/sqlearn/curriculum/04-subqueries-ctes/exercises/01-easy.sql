-- TASK: Find all runs where the distance_km is greater than the overall average distance.
-- Show activity_date, distance_km, and avg_pace_min_per_km. Order by distance descending.
-- TABLE: site_runs
-- HINT: Use a scalar subquery in WHERE: SELECT AVG(distance_km) FROM site_runs.

-- YOUR SOLUTION BELOW:
