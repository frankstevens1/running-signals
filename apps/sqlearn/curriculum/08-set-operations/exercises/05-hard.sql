-- TASK: Combine runs from two different distance buckets (e.g., '5-10' and '10-15'),
-- then compute average pace per bucket across the combined set.
-- Show route_distance_bucket_km, run count, and average pace.
-- TABLE: site_runs
-- HINT: Use UNION ALL as a subquery, then GROUP BY on the outer query.

-- YOUR SOLUTION BELOW:
