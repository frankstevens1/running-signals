-- TASK: Combine all runs from 2024 and 2025 into a single result set (deduplicated).
-- Show activity_date, distance_km, and avg_pace_min_per_km. Order by activity_date.
-- TABLE: site_runs (two subqueries)
-- HINT: Use SELECT ... WHERE year=2024 UNION SELECT ... WHERE year=2025.

-- YOUR SOLUTION BELOW:
