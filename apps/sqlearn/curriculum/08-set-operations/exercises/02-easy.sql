-- TASK: Same as exercise 01 but using UNION ALL. Notice the difference in row counts.
-- Compare the total row counts of UNION vs UNION ALL for the same query.
-- TABLE: site_runs
-- HINT: Use UNION ALL instead of UNION. UNION removes duplicates; UNION ALL does not.

-- YOUR SOLUTION BELOW:
