-- TASK: Find data quality inconsistencies — runs where avg_heart_rate IS NULL but
-- max_heart_rate IS NOT NULL (we have max HR but not average?).
-- Show activity_date, avg_heart_rate, max_heart_rate.
-- TABLE: site_runs
-- HINT: WHERE avg_heart_rate IS NULL AND max_heart_rate IS NOT NULL.

-- YOUR SOLUTION BELOW:
