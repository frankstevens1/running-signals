-- TASK: Compute the data completeness percentage for each column in site_runs.
-- Show column name, non_null_count, total_count, and completeness_pct (1 decimal).
-- Do this for: avg_heart_rate, max_heart_rate, avg_pace_min_per_km, total_ascent.
-- TABLE: site_runs
-- HINT: Use COUNT(column) * 100.0 / COUNT(*) for each column. You can do this in a single SELECT.

-- YOUR SOLUTION BELOW:
