-- TASK: Count how many runs have NULL avg_heart_rate vs non-NULL.
-- Show total_runs, runs_with_hr, runs_without_hr.
-- TABLE: site_runs
-- HINT: COUNT(*) for total, COUNT(avg_heart_rate) for non-null. Subtract for NULL count.

-- YOUR SOLUTION BELOW:
