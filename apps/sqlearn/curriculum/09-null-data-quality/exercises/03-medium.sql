-- TASK: Create a complete run summary that replaces ALL NULL metrics with sensible defaults.
-- Replace NULL heart_rate with 0, NULL total_ascent with 0, NULL avg_pace_min_per_km with 0.
-- Show activity_date and all 4 columns for the 10 most recent runs.
-- TABLE: site_runs
-- HINT: Use COALESCE() on each nullable column.

-- YOUR SOLUTION BELOW:
