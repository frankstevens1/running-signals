-- TASK: Find personal records: the longest run and the fastest run for each year.
-- Show year, activity_date, distance_km, avg_pace_min_per_km, and the record type ("Longest" or "Fastest").
-- TABLE: site_runs
-- HINT: Use ROW_NUMBER() twice with different ORDER BY, filter for rank=1. UNION the results.

-- YOUR SOLUTION BELOW:
