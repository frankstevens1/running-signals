-- TASK: Analyze how pace changes with distance. Group runs into distance buckets (rounded to integer km)
-- and compute average pace and run count per bucket. Only show buckets with 2+ runs.
-- Show distance_bucket, run_count, and avg_pace. Order by distance_bucket.
-- TABLE: site_runs
-- HINT: Use ROUND(distance_km) as the bucket, GROUP BY, HAVING COUNT(*) >= 2.

-- YOUR SOLUTION BELOW:
