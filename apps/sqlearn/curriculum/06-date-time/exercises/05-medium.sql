-- TASK: Find the most active day of the week (by total distance covered).
-- Show the day-of-week number and total distance. Limit to 1 row (the top day).
-- TABLE: site_runs
-- HINT: GROUP BY EXTRACT(DOW FROM activity_date), ORDER BY SUM(distance_km) DESC, LIMIT 1.

-- YOUR SOLUTION BELOW:
