-- TASK: Group runs by week (using DATE_TRUNC) and compute total distance and run count per week.
-- Show week_start, total_km (1 decimal), and run_count. Order by week_start.
-- TABLE: site_runs
-- HINT: Use DATE_TRUNC('week', activity_date) in both SELECT and GROUP BY.

-- YOUR SOLUTION BELOW:
