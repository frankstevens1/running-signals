-- TASK: Group runs by day of the week (0=Sunday, 6=Saturday).
-- Show the day-of-week number, run count, and average distance. Order by DOW.
-- TABLE: site_runs
-- HINT: Use EXTRACT(DOW FROM activity_date) in both SELECT and GROUP BY.

-- YOUR SOLUTION BELOW:
