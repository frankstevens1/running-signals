-- TASK: Extract year and month from activity_date and count runs per month.
-- Show year, month number, and run count. Order by year, then month.
-- TABLE: site_runs
-- HINT: Use EXTRACT(YEAR FROM activity_date) and EXTRACT(MONTH FROM activity_date) in both SELECT and GROUP BY.

-- YOUR SOLUTION BELOW:
