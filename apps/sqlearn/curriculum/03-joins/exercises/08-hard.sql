-- TASK: For each run, compare its pace to the route's overall average pace.
-- Show activity_date, run pace, route average pace, and the difference.
-- Only show runs where the individual pace differs by more than 0.25 from the route avg.
-- TABLE: site_runs JOIN site_routes
-- HINT: INNER JOIN on route_id. Calculate difference = r.avg_pace_min_per_km - rt.avg_pace_min_per_km.

-- YOUR SOLUTION BELOW:
