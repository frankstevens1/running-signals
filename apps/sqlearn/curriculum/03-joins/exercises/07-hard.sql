-- TASK: Multi-table join: show each run with route context and fitness signals in one query.
-- Include activity_date, distance_km, route run_count, and fitness efficiency_ratio.
-- Order by activity_date descending, limit 20.
-- TABLE: site_runs JOIN site_routes JOIN site_fitness
-- HINT: INNER JOIN site_routes ON route_id, LEFT JOIN site_fitness ON activity_id.

-- YOUR SOLUTION BELOW:
