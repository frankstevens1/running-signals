-- TASK: LEFT JOIN site_days with site_runs to show ALL days (including rest days with no runs).
-- Show calendar_date, day distance_km, and the activity_date of any runs on that day.
-- Include up to 30 recent days.
-- TABLE: site_days LEFT JOIN site_runs
-- HINT: LEFT JOIN d.calendar_date = r.activity_date. Rows without runs will have NULL run data.

-- YOUR SOLUTION BELOW:
