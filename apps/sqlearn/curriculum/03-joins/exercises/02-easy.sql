-- TASK: LEFT JOIN site_runs with site_fitness to show all runs with their fitness metrics.
-- Show activity_date, distance_km, efficiency_ratio, and hr_drift_pct.
-- Order by activity_date descending, limit 20.
-- TABLE: site_runs LEFT JOIN site_fitness
-- HINT: LEFT JOIN ON r.activity_id = f.activity_id.

-- YOUR SOLUTION BELOW:
