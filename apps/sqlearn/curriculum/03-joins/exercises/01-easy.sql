-- TASK: INNER JOIN site_runs with site_routes to show each run with its route's historical statistics.
-- Show activity_date, distance_km, route_id, route run_count, and route avg_distance_km.
-- Order by activity_date descending, limit 15.
-- TABLE: site_runs JOIN site_routes
-- HINT: JOIN ON r.route_id = rt.route_id.

-- YOUR SOLUTION BELOW:
