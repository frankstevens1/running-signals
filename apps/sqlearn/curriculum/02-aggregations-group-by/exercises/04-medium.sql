-- TASK: For each route_distance_bucket_km, show the run count, average distance (2 decimals),
-- and average heart rate (1 decimal). Only include buckets with 3 or more runs.
-- Sort by run count descending.
-- TABLE: site_runs
-- HINT: Use GROUP BY with HAVING COUNT(*) >= 3.

-- YOUR SOLUTION BELOW:
