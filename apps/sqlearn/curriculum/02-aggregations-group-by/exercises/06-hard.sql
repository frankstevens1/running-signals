-- TASK: Compare your average distance per run on weekdays (Mon-Fri) vs weekends (Sat-Sun).
-- Show the day type (weekday/weekend), run count, average distance (2 decimals),
-- and average pace (2 decimals).
-- TABLE: site_runs
-- HINT: Use CASE with EXTRACT(DOW FROM activity_date). DOW: 0=Sun, 1=Mon, ..., 6=Sat.
-- Group by the CASE expression.

-- YOUR SOLUTION BELOW:
