-- TASK: For each year, calculate the number of runs, total distance (rounded to 1 decimal),
-- and average pace (rounded to 2 decimals). Sort by year ascending.
-- TABLE: site_runs
-- HINT: Use EXTRACT(YEAR FROM activity_date) in both SELECT and GROUP BY.

-- YOUR SOLUTION BELOW:
